package com.radiotelescope.contracts.accountActivateToken

/**
 * Enum representing field validation failures for the AccountActivateToken Entity
 */
enum class ErrorTag {
    TOKEN,
    EXPIRATION_DATE
}