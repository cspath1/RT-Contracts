package com.radiotelescope.contracts.viewer

/**
 * Enum representing field validation failures for the Viewer Entity
 */
enum class ErrorTag {
    ID,
    USER_ID,
    APPOINTMENT_ID,
    PRIVATE,
    PAGE_PARAMS
}